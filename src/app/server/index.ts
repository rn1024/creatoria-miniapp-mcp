/**
 * Temporary server facade for the new app layer.
 * Existing runtime continues to live in ../../server.ts.
 */

export { startServer } from '../../server.js'
export type { ServerConfig } from '../../types.js'
